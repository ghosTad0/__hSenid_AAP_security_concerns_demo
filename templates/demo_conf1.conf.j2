server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;
    index {{ demo_html_filename }};

    server_name {{ hostvars[inventory_hostname].ansible_host }};  # or use your domain name if applicable

    location / {
        try_files $uri $uri/ =404;
    }
}